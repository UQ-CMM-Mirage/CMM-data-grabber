<?xml version="1.0" encoding="UTF-8"?>
<Context>
    <!-- The 'Paul' webapp security model recognizes ordinary users (role "ROLE_USER") 
         and administrators (role "ROLE_ADMIN").  Administrator accounts must be 
         configured in "$CATALINA_HOME/conf/tomcat-users.xml".  Ordinary user 
         accounts are typically taken from ACLS ... via the custom ACLSRealm. 
    -->
    <Realm className="org.apache.catalina.realm.LockOutRealm">
        <Realm className="org.apache.catalina.realm.MemoryRealm"/>
    	<Realm className="au.edu.uq.cmm.tomcat.realm.ACLSRealm" 
           serverHost="152.98.195.10" serverPort="1024" 
           dummyFacility="The Proxy" localHostId="FISH"/>
    </Realm>
    
</Context>