<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Scans within the base package of the application for @Components to 
		configure as beans -->
	<!-- @Controller, @Service, @Configuration, etc. -->
	<context:component-scan base-package="au.edu.uq.cmm.paul.servlet" />

	<!-- Enables the Spring MVC @Controller programming model -->
	<mvc:annotation-driven />


	<bean class="org.springframework.web.servlet.view.BeanNameViewResolver">
		<property name="order">
			<value>1</value>
		</property>
	</bean>
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- Resolve logical view names to .jsp resources in the /WEB-INF/views 
			directory -->
		<property name="prefix" value="/WEB-INF/jsp/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<bean class="au.edu.uq.cmm.paul.Paul" name="services" init-method="startup"
		destroy-method="shutdown">
		<!-- Note that the static config is only used if the dynamic config loaded 
			from the database is empty / uninitialized -->
		<constructor-arg ref="staticConfig" />
		<constructor-arg ref="entityManagerFactory" />
		<constructor-arg>
			<bean class="au.edu.uq.cmm.paul.status.LashupAclsAccountMapper" />
		</constructor-arg>
	</bean>

	<bean class="au.edu.uq.cmm.aclslib.server.StaticConfiguration"
		factory-method="loadConfiguration" name="staticConfig">
		<constructor-arg value="/home/scrawley/config.json" />
	</bean>

	<bean id="fileView" class="au.edu.uq.cmm.paul.servlet.FileView">
	</bean>

</beans>
